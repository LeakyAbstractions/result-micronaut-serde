// Configure Javadoc
javadoc {
    options {
        title project.artifactName
        header "${project.artifactName} ${rootProject.version}"
        author true
        links 'https://docs.oracle.com/javase/8/docs/api/'
        links "https://dev.leakyabstractions.com/result/javadoc/${libs.versions.result.core.get()}/"
    }
}
